{
  "id": "openclaw-sandcastle",
  "name": "Sandcastle",
  "description": "Lightweight ephemeral bwrap filesystem sandbox for OpenClaw agents (secret-path isolation, no Docker).",
  "version": "0.1.4",
  "kind": "sandbox",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "mapDir": {
        "type": "array",
        "items": {
          "type": "string"
        },
        "description": "Glob map rules: [prefix]<path>[:mode]. + force-allow, - deny (wins). Merged global + per-agent."
      },
      "env": {
        "type": "object",
        "additionalProperties": {
          "anyOf": [
            {
              "type": "boolean"
            },
            {
              "type": "string"
            }
          ]
        },
        "description": "Env schema: true = pass host value, false = strip, string = literal. Unlisted vars are stripped (deny-by-default)."
      }
    }
  }
}
